// Auto-generated by UVM-AI
class {{ name }} extends uvm_driver #({{ transaction_type }});

  `uvm_component_utils({{ name }})

  virtual {{ interface_name }} vif;

  function new(string name = "{{ name }}", uvm_component parent = null);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    if (!uvm_config_db#(virtual {{ interface_name }})::get(this, "", "vif", vif))
      `uvm_fatal(get_name(), "Could not get virtual interface")
  endfunction

  task run_phase(uvm_phase phase);
    {{ transaction_type }} req;
    forever begin
      seq_item_port.get_next_item(req);
      drive_item(req);
      seq_item_port.item_done();
    end
  endtask

  task drive_item({{ transaction_type }} item);
    {% if drive_logic_hint %}
    // {{ drive_logic_hint }}
    {% endif %}
    @(posedge vif.clk);
    // TODO: Implement drive logic for {{ interface_name }}
    `uvm_info(get_name(), $sformatf("Driving: %s", item.convert2string()), UVM_HIGH)
  endtask

endclass

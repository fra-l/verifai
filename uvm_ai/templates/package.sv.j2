// Auto-generated by UVM-AI
package {{ package_name }};

  import uvm_pkg::*;
  `include "uvm_macros.svh"

  // Sequence items
  {% for item in sequence_items %}
  `include "{{ item }}"
  {% endfor %}

  // Sequences
  {% for seq in sequences %}
  `include "{{ seq }}"
  {% endfor %}

  // Agents (driver, monitor, sequencer, agent)
  {% for agent_file in agent_files %}
  `include "{{ agent_file }}"
  {% endfor %}

  // Scoreboards
  {% for sb in scoreboards %}
  `include "{{ sb }}"
  {% endfor %}

  // Environment
  {% for env in environments %}
  `include "{{ env }}"
  {% endfor %}

  // Tests
  {% for test in tests %}
  `include "{{ test }}"
  {% endfor %}

endpackage

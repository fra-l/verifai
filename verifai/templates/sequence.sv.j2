// Auto-generated by verifai
class {{ name }} extends uvm_sequence #({{ transaction_type }});

  `uvm_object_utils({{ name }})

  int unsigned num_items = {{ num_items }};

  function new(string name = "{{ name }}");
    super.new(name);
  endfunction

  task body();
    {% if body_logic_hint %}
    // {{ body_logic_hint }}
    {% endif %}
    `uvm_info(get_name(), $sformatf("Starting sequence with %0d items", num_items), UVM_MEDIUM)
    repeat (num_items) begin
      {{ transaction_type }} item;
      item = {{ transaction_type }}::type_id::create("item");
      start_item(item);
      if (!item.randomize())
        `uvm_error(get_name(), "Randomization failed")
      finish_item(item);
    end
    `uvm_info(get_name(), "Sequence complete", UVM_MEDIUM)
  endtask

endclass

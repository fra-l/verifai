// Auto-generated by verifai
class {{ name }} extends uvm_scoreboard;

  `uvm_component_utils({{ name }})

  uvm_analysis_imp #({{ transaction_type }}, {{ name }}) analysis_imp;

  {{ transaction_type }} expected_queue[$];
  int unsigned match_count;
  int unsigned mismatch_count;

  function new(string name = "{{ name }}", uvm_component parent = null);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    analysis_imp = new("analysis_imp", this);
  endfunction

  function void write({{ transaction_type }} txn);
    {% if check_logic_hint %}
    // {{ check_logic_hint }}
    {% endif %}
    // TODO: Implement comparison logic
    `uvm_info(get_name(), $sformatf("Received: %s", txn.convert2string()), UVM_HIGH)
  endfunction

  function void report_phase(uvm_phase phase);
    super.report_phase(phase);
    `uvm_info(get_name(), $sformatf("Matches: %0d, Mismatches: %0d", match_count, mismatch_count), UVM_LOW)
    if (mismatch_count > 0)
      `uvm_error(get_name(), $sformatf("%0d mismatches detected!", mismatch_count))
  endfunction

endclass
